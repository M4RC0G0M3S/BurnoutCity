namespace BurnoutCity.Core
{
    // Placeholder — lógica de colisões centralizada ainda não implementada.
    // Atualmente as colisões são tratadas diretamente nos estados (ExplorationState, RaceState).
    public class CollisionManager { }
}